# 问弦 - 安装说明

## 一、单机使用（个人电脑）

### 1.1 环境要求

| 项目 | 要求 |
|------|------|
| 操作系统 | Windows 10 / 11（64位） |
| Node.js | v18 或更高版本（[下载](https://nodejs.org)） |
| Ollama | 最新版（[下载](https://ollama.com)） |
| 显存（可选） | 6GB 可流畅运行 7B 模型，无独立显卡也可用 CPU 模式 |
| 磁盘空间 | 系统本身约 10MB，模型文件需 4-8GB |
| 字体（PDF导出） | 需要微软雅黑字体（Windows 默认已安装） |

### 1.2 安装步骤

#### 第一步：安装 Node.js

1. 访问 https://nodejs.org
2. 下载 **LTS（长期支持版）** 安装包
3. 双击安装，全程默认下一步即可
4. 安装完成后打开命令提示符（Win+R 输入 `cmd`），输入以下命令验证：
   ```
   node --version
   ```
   显示类似 `v20.x.x` 即为成功

#### 第二步：安装 Ollama

1. 访问 https://ollama.com
2. 下载 Windows 版安装包并安装
3. 安装完成后 Ollama 会自动启动（任务栏右下角出现 Ollama 图标）

#### 第三步：下载大语言模型

打开命令提示符，执行以下命令下载模型（以推荐的 qwen2.5:7b 为例）：

```
ollama pull qwen2.5:7b
```

下载时间取决于网速，模型文件约 4.7GB。下载完成后可验证：

```
ollama list
```

看到 `qwen2.5:7b` 即表示成功。

**其他可选模型：**

| 显存 | 推荐模型 | 下载命令 |
|------|---------|---------|
| 4GB 或无显卡 | qwen2.5:3b | `ollama pull qwen2.5:3b` |
| 6GB | qwen2.5:7b | `ollama pull qwen2.5:7b` |
| 8GB | qwen2.5:7b | `ollama pull qwen2.5:7b` |
| 12GB+ | qwen2.5:14b | `ollama pull qwen2.5:14b` |

#### 第四步：解压并启动系统

1. 将收到的 `离线文本处理` 压缩包解压到任意目录（路径中建议不要包含中文）
2. 双击文件夹中的 `start.bat`
3. 脚本会自动完成以下操作：
   - 检查端口占用并释放
   - 检查 Node.js 和 Ollama 是否就绪
   - 首次运行时自动安装依赖（约 1-2 分钟）
   - 启动开发服务器
   - 自动打开浏览器访问 http://localhost:3000
4. 看到浏览器中显示系统界面即为启动成功

> **注意：** 启动过程中如果提示 Ollama 未运行，脚本会尝试自动启动 Ollama。如果自动启动失败，请手动双击 Ollama 图标或在命令行运行 `ollama serve`。

### 1.3 常见问题

**Q: 双击 start.bat 后闪退或出现乱码？**
A: 右键点击 start.bat → 选择"以管理员身份运行"。如果仍然闪退，请在 start.bat 所在文件夹的地址栏输入 `cmd` 回车，然后输入 `start.bat` 查看具体报错信息。

**Q: 浏览器打开后提示"Ollama 未连接"？**
A: 确认 Ollama 已启动（任务栏右下角有图标），然后在系统设置中点击"测试连接"。如果仍然失败，尝试重启 Ollama。

**Q: 模型回复很慢？**
A: 如果没有独立显卡，CPU 模式下 7B 模型回复较慢属正常现象。建议换用 `qwen2.5:3b` 小模型：`ollama pull qwen2.5:3b`，然后在设置中切换模型。

**Q: 导出 PDF 时中文显示异常？**
A: 系统优先使用微软雅黑字体渲染 PDF。如果电脑上没有微软雅黑（部分精简版系统），请安装微软雅黑字体或任意支持中文的字体，然后重启浏览器即可。

**Q: AI 回答被截断了怎么办？**
A: 系统已内置自动续写功能，如果检测到回答被截断会自动继续生成。如果仍然不完整，可以让 AI "继续" 或 "请补充完整"，它会基于上下文继续回答。

**Q: 文档编辑器的纠错/润色/优化功能很慢？**
A: 这些功能使用流式输出，点击后会立即看到文本开始变化。首次使用时模型加载可能需要 1-2 分钟。如果使用 CPU 模式（无独立显卡），生成速度会较慢，但不会超时中断（超时时间为 1 小时）。

**Q: 纠错功能总是显示"没有错误"？**
A: 如果文档确实没有明显错误，AI 会返回"未发现明显错误"。如果确定有错误但未检出，请检查：1) 文档内容是否过短（建议 200 字以上）；2) 浏览器控制台（F12）是否有错误日志。

---

## 二、局域网服务器部署（多用户共享）

将系统部署在一台服务器上，局域网内其他电脑通过浏览器访问即可使用，每个用户的对话互不影响。

### 2.1 服务器要求

| 项目 | 要求 |
|------|------|
| 操作系统 | Windows 10/11 或 Windows Server |
| 显卡 | 建议 12GB+ 显存以支持多用户并发 |
| 网络 | 服务器与客户端在同一局域网内 |
| 端口 | 3000（Web 界面）、11434（Ollama API）需开放 |

### 2.2 部署步骤

#### 第一步：服务器端安装

在服务器上完成以下操作（与单机使用相同）：

1. 安装 Node.js（v18+）
2. 安装 Ollama
3. 下载模型：
   ```
   ollama pull qwen2.5:7b
   ```
4. 将 `离线文本处理` 文件夹放到服务器任意目录

#### 第二步：启动服务

双击 `start.bat` 启动系统。启动成功后会显示：

```
Local:  http://localhost:3000
LAN:    http://192.168.x.x:3000
```

记下 LAN 地址（如 `http://192.168.1.100:3000`），这就是其他用户访问的地址。

#### 第三步：客户端访问

1. 在任意电脑的浏览器（推荐 Chrome / Edge）中输入服务器的 LAN 地址
2. 打开后点击左下角 **设置**
3. 在 **本地模型 (Ollama)** 区域，将服务地址从 `http://localhost:11434` 改为：
   ```
   http://服务器IP:11434
   ```
   例如：`http://192.168.1.100:11434`
4. 点击 **测试连接**，显示成功即可正常使用

#### 第四步：防火墙配置（如有访问问题）

如果客户端无法访问，需要在服务器的 Windows 防火墙中放行端口：

1. 打开 **Windows Defender 防火墙** → **高级设置**
2. 选择 **入站规则** → **新建规则**
3. 选择 **端口**，下一步
4. 输入端口号：`3000,11434`
5. 选择 **允许连接**，完成

或者以管理员身份运行以下命令：

```powershell
netsh advfirewall firewall add rule name="SmartText-Web" dir=in action=allow protocol=tcp localport=3000
netsh advfirewall firewall add rule name="SmartText-Ollama" dir=in action=allow protocol=tcp localport=11434
```

### 2.3 多用户隔离说明

- 每个用户的对话历史保存在各自的浏览器 localStorage 中，互不干扰
- 用户之间不会看到彼此的对话内容
- 所有用户共享同一个 Ollama 实例和模型，同时使用时可能出现排队现象（取决于服务器显存和模型大小）
- 如果并发用户较多（5人以上），建议使用更大的模型如 `qwen2.5:14b`，或在多台服务器上分别部署

### 2.4 局域网部署注意事项

| 项目 | 说明 |
|------|------|
| Ollama CORS | start.bat 已自动设置 `OLLAMA_ORIGINS=*`，无需手动配置 |
| 模型推荐 | 多用户场景建议使用 qwen2.5:7b 或更大模型，避免使用 3b 小模型 |
| 显存不足 | 如多用户并发时显存不足，Ollama 会自动降级到 CPU 推理，速度会明显变慢 |
| 端口冲突 | 如果 3000 端口被占用，start.bat 会自动释放。如需使用其他端口，修改 `vite.config.ts` 中的 `port` 值 |
| 开机自启 | 可将 start.bat 的快捷方式放入 Windows 启动文件夹（`shell:startup`）实现开机自动启动 |

---

## 三、文件结构说明

```
离线文本处理/
├── start.bat              # 一键启动脚本（双击即可）
├── package.json           # 项目配置
├── vite.config.ts         # 构建/开发服务器配置
├── index.html             # 入口页面
├── .env.example           # 环境变量示例
├── .gitignore
├── src/                   # 源代码
│   ├── views/             # 页面组件
│   ├── components/        # 通用组件
│   ├── stores/            # 状态管理
│   ├── services/          # 核心服务（AI 引擎、文件解析）
│   ├── utils/             # 工具函数
│   ├── api/               # API 接口
│   └── types/             # TypeScript 类型定义
├── public/                # 静态资源
└── node_modules/          # 依赖（首次运行自动生成）
```

> **注意：** `node_modules/` 文件夹不包含在压缩包中，首次运行 start.bat 时会自动执行 `npm install` 生成。如果自动安装失败，请在命令行中进入项目目录手动执行 `npm install`。
